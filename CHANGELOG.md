# CHANGELOG — 오늘, 우리 사진관 (ourstudio-today-sample)

## 2026-07-31 — v0.7 Pretendard self-host (CF 이메일난독화 회피)
- 원인: CF Email Obfuscation이 jsdelivr `[email protected]` URL의 `[email protected]`을 이메일로 오인→`[email protected]` 치환→CSS 400→Pretendard 미로드(시스템 폴백). ([[cloudflare-email-obfuscation-cdn-url]])
- 해결: CDN `<link>` 제거 → **PretendardVariable.ttf를 페이지 사용 글리프(467자)로 서브셋한 105KB woff2 self-host** + `@font-face`(font-weight:100 900 variable, display swap). @version URL이 사라져 CF가 못 건드림.
- Gowun Batang은 정상 로드 중이라 CDN 유지(납품 시 self-host 대상). pretendard-sub.woff2 커밋.


## 2026-07-31 — v0.6 코드 검토 반영(견고성·성능·SEO)
- **[버그] JS-off 백지 방지**: `.rv{opacity:0}`가 `.js` 게이팅·noscript 없이 걸려 JS 꺼지면 본문 전체 안 보이던 회귀 → `<noscript><style>.rv{opacity:1!important;transform:none!important}</style></noscript>` 추가. (JS 경로는 IO+1.6s+else로 견고, JS-off만 뚫려 있었음. eunasoo·daynone 폴백의 회귀)
- **[성능] 마퀴 우선순위**: 마퀴 12장 eager가 히어로 LCP와 경쟁 → **fetchpriority="low" + decoding="async"**로 뒤로 미룸. ⚠️lazy는 transform 마퀴에서 화면밖 이미지가 안 뜰 위험이라 미채택(우선순위 낮춤으로 대체).
- **[SEO] JSON-LD 추가**: Organization(오우사, sameAs 인스타·블로그) + 주소 확인된 용리단길점만 PhotographyBusiness/LocalBusiness. 나머지 9곳은 주소 미확인이라 미포함(지어내지 않음).


## 2026-07-31 — v0.5 모바일 미세조정
- 소개 피처 카드(색감·원본·반려동물·인원): 모바일에서 아이콘 위·텍스트 아래 **중앙 정렬**(우측 쏠림 해결).
- 색감 5타일 홀수 문제: 모바일 2열에서 **5번째(베이지) 타일을 가로 전체 span(4:3)**으로 딱 맞춤.
- "예약 전 꼭 확인해 주세요" 하단에 **인스타그램 촬영 예시 보기 버튼** 추가.
- 피처 카드 우측 쏠림 실제 원인=`.feat` ul의 **브라우저 기본 padding-left:40px** → padding:0 리셋(중앙정렬 규칙과 별개 버그).
- PC 소개 카드가 너무 상단에 붙던 문제: `.about-in` 데스크톱 `align-items:start`→`center`(왼쪽 본문 높이에 맞춰 세로 중앙).


## 2026-07-31 — v0.4 (다올 피드백 2차)
- **피처 아이콘**: 한자(色·全·友·今) → 의미 없는 SVG 문양(새싹·8각별·동심원·마름모).
- **색감 예시**: 사장님 라벨 이미지로 교체 — 클래식/비비드/베이지=신규 t-*.jpg(클래식.jpeg·비비드8.png·베이지1.jpeg), 모노톤=os-36(모노톤1), 파스텔=os-hero(파스텔1).
- **지점**: 10개를 **클릭 버튼(a)**으로, 설명형 이름(예: "대학로 옆 마로니에점")+각 naver.me 링크 연결. 호버 인터랙션.
- **예약 버튼**: 모든 "네이버 예약" CTA → **대학로점(마로니에점) naver.me/IFgdfdO0**로 통일(기존 삼각지 place 링크 제거).
- **촬영 가이드**: 이용 가이드 섹션에 **토글(details)** 추가(입장·시간배분·리모컨·색감·의상·포즈·인원 7항목). ⚠️ 블로그 크롤 불가라 확인된 사실 기반으로 작성 → 블로그 원문 주면 교체 예정.


## 2026-07-31 — v0.3 대개편(다올 피드백)
- **폰트**: Nanum Myeongjo(두꺼움) → **Gowun Batang**(얇고 단정·따뜻), 헤딩 weight 400.
- **히어로 사진**: 파스텔1(초록 화관 인물, 정사각)로 교체. 프레임 태그 "용산·삼각지" → **"전국 10개 지점"**.
- **로고**: 흐린 추출본 → 사장님 제공 **logo1(워드마크)·logo2(마크)** self-host, 헤더에 선명 표시.
- **갤러리**: CSS columns 마소너리(하단 빈칸 문제) → **균일 그리드(aspect 4/5)** 36타일, 5컨셉(클래식·모노톤·비비드·파스텔·베이지) 고르게. 헤딩 "수많은 오늘의 기록"→**"오우사 포트폴리오"**.
- **신규 섹션**: 다섯 가지 색감(컨셉 예시 5), 이용 가이드(50분 타임라인 3스텝+블로그 링크), FAQ 6문항(인스타 하이라이트 Q&A 기반), 전국 10개 지점.
- **이용안내**: 예약 전 확인 = 토글 제거 → **상시 노출**.
- **스크롤**: 앵커 클릭 = rAF 커스텀 스무스(동작 줄이기에도 강제, 헤더 오프셋).
- **문구 수정**: 1~7인→최대 7인, 소개 "…스튜디오입니다.", 가격 h2 "50분의 셀프 촬영", 히어로 마침표 제거.
- ⚠️ 블로그(blog.naver) 크롤 불가(네이버 안전차단) → FAQ는 첨부 하이라이트 Q&A로 구성. 지점별 상세주소·네이버 예약 링크는 삼각지(1639948343) 1곳만 확인됨(나머지 미확인).


## 2026-07-31 — v0.2 배경 순백 전환
- 다올 요청: 배경 베이지(아이보리) → **순백(#fff)**. 베이지는 **테두리·구분선·포인트**로만. 그린 액센트는 유지.
- 팔레트: --bg #F7F4ED→#fff, --line/--line2를 뉴트럴 rgba→**베이지 라인(#e4dbc6 / #efe8d9)**. 헤더·드로어·퀵바 배경 흰색화. .about/.price/.loc의 베이지 그라데이션 제거→#fff + 베이지 상단 구분선. #gallery·#info에도 구분선. 카드(feat·info) 테두리 베이지 강화.


## 2026-07-31 — v0.1 최초 빌드
- 용산 삼각지 셀프 사진관 원페이지 시안 제작.
- 구성: 헤더(스티키)→히어로(분할: 카피+가족 컷)→소개→마퀴 스트립→하나의 큰 갤러리(34컷·다양한 색감)→가격→이용안내(+예약 전 확인 아코디언)→오시는 길→푸터 + 모바일 퀵바.
- 디자인: 화이트/아이보리 베이스 + 브랜드 그린(#5E7249) 액센트 + 웜 near-black 잉크. 폰트=Nanum Myeongjo(명조 디스플레이·CDN) + Pretendard(본문·CDN). 로고 self-host(투명 PNG).
- 이미지: 인스타 원본 107장 중 34컷+히어로 self-host(리사이즈·최적화, 슬러그 os-01~34/os-hero).
- 모션: 마퀴=setInterval 강제 구동, 리빌=IO+1.6s 안전망(동작 줄이기에도 강제 재생, 다올 지정).
- CTA=네이버 예약(플레이스) 일관(PC 메인=모바일 퀵바). 전화번호 없음(무인 스튜디오).
- 미확정/주의: 네이버 "예약" 직링크 대신 플레이스 홈 링크 사용(예약금·정확한 booking URL 미확인). 폰트는 시안이라 CDN(납품 시 self-host 전환).
